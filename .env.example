# =============================================================================
# CONFIGURACIÓN DE ENTORNO - STREAMBUS
# =============================================================================
# INSTRUCCIONES:
# 1. Copia este archivo a .env: cp .env.example .env
# 2. Completa los valores con tus credenciales reales
# 3. NUNCA subas .env al repositorio (debe estar en .gitignore)

# =============================================================================
# DJANGO CORE
# =============================================================================
DJANGO_SECRET_KEY=tu-secret-key-aqui-generala-con-get-random-secret-key
DEBUG=False
ALLOWED_HOSTS=127.0.0.1,localhost,tudominio.com

# =============================================================================
# BASE DE DATOS PRINCIPAL
# =============================================================================
DB_NAME=streambus
DB_USER=sa
DB_PASSWORD=tu-password-aqui
DB_HOST=190.183.254.254
DB_PORT=62593
DB_DRIVER=ODBC Driver 17 for SQL Server

# =============================================================================
# BASE DE DATOS SIT (SECUNDARIA)
# =============================================================================
DB_SIT_NAME=SIT
DB_SIT_USER=wwwStreamBusSitR
DB_SIT_PASSWORD=tu-password-sit-aqui
DB_SIT_HOST=190.183.254.254
DB_SIT_PORT=62593

# =============================================================================
# EMAIL
# =============================================================================
EMAIL_HOST=192.168.250.250
EMAIL_PORT=25
EMAIL_USE_SSL=False
EMAIL_USE_TLS=False
EMAIL_HOST_USER=cristian.gudino
EMAIL_HOST_PASSWORD=tu-password-email-aqui
DEFAULT_FROM_EMAIL=sit.autobuses@autobusessantafe.com.ar

# =============================================================================
# GPS/CITOS
# =============================================================================
GPS_BASE_URL=http://190.183.254.253:8088
GPS_TIMEOUT=30
GPS_VERIFY_SSL=True
GPS_ACCOUNT=admin
GPS_PASSWORD=tu-password-gps-aqui
USE_CITOS_LIBRARY=False

# =============================================================================
# ARCHIVOS Y MEDIA
# =============================================================================
DATA_UPLOAD_MAX_MEMORY_SIZE=524288000
MAX_VIDEO_UPLOAD_SIZE_MB=60
FILE_UPLOAD_MAX_MEMORY_SIZE=52428800

# =============================================================================
# CELERY / REDIS
# =============================================================================
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/0

# =============================================================================
# OPTIMIZACIÓN DE DESCARGAS
# =============================================================================
MAX_DOWNLOAD_WORKERS=25
MAX_CONCURRENT_DOWNLOADS=10
HTTP_TIMEOUT=40
API_BATCH_SIZE=200
INITIAL_POLL_INTERVAL=8000

# =============================================================================
# STORAGE (S3/MinIO) - OPCIONAL
# =============================================================================
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_STORAGE_BUCKET_NAME=
# AWS_S3_REGION_NAME=us-east-1
# USE_S3_STORAGE=False
